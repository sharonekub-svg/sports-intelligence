import { describe, it, expect } from "vitest";

// Placeholder confirming the Vitest harness + @/ path alias resolve
// correctly. Real prediction-engine unit tests (devig, Poisson,
// Dixon-Coles, Elo, Wilson, shrinkage, calibration, opportunity score)
// land alongside their modules in the next build phase.
describe("test harness", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
