/**
 * Placeholder for generated Supabase types.
 *
 * Once the real Supabase project exists and migrations are applied, replace
 * this file with the generated output:
 *
 *   pnpm supabase gen types typescript --project-id <ref> > lib/supabase/types.ts
 *
 * or, against the local dev stack:
 *
 *   pnpm supabase gen types typescript --local > lib/supabase/types.ts
 *
 * Until then, `Database` is loosely typed so the rest of the app compiles;
 * table-shaped code should still match `supabase/migrations/*.sql` by hand.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
